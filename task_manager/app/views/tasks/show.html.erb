<h1>id:<%= @task.id %>のタスク詳細</h1>

<p>タスク名:<%= @task.name %></p>

<p>メモ:<%= @task.description %></p>

<p>締め切り:<%= @task.due_at %></p>

<p>優先度:<%= @task.priority_i18n %></p>

<p>進捗状況:<%= @task.progress_i18n %></p>

<%= link_to '一覧へ戻る',tasks_path %>

<%= link_to '編集ページへ', edit_task_path(@task) %>

<%= link_to '削除する', @task, method: :delete, id: "delete_button", data:{ confirm: '本当によろしいですか？'} %>
