<h1><%= I18n.t 'tasks.index.title' %></h1>
<div class="row">
  <div class="col-6">
    <%= form_with url: root_path, method: :get, local: true do |f| %>
        <%= f.text_field :keyword_name, value: @keyword_name ,:placeholder => (t '.name') %>
        <%= f.select :keyword_progress, options_for_select(Task.progresses_i18n.keys.map {|k| [I18n.t("enums.task.progress.#{k}"), k]}, selected: @keyword_progress) , { include_blank: '進捗状況'} %>
        <%= f.select("label_id", Label.pluck(:name, :id), { include_blank: 'ラベル名' }) %>
        <%= f.submit "検索" %>
    <% end %>
  </div>
  <div class="col-6">
    <%= paginate @tasks %>
  </div>
</div>
<table class="table table-striped table-bordered table-hover" style="margin-top: 10px;">
  <thead>
    <tr>
      <th scope="col"><%= build_sort_link "name" , (I18n.t 'tasks.index.name')%></th>
      <th scope="col"><%= build_sort_link "created_at" , (I18n.t 'tasks.index.created_at')%></th>
      <th scope="col"><%= build_sort_link "due_at" , (I18n.t 'tasks.index.due_at')%></th>
      <th scope="col"><%= build_sort_link "progress" , (I18n.t 'tasks.index.progress')%></th>
    </tr>
  </thead>

  <tbody>
    <% @tasks.each_with_index do |task , index| %>
      <tr id= 'task-<%= index %>'>
        <td style="padding-top: 5px;padding-bottom: 5px;">
          <div>
            <% task.labels.pluck(:name).each do |label_name|%>
              <span class="badge badge-pill badge-primary"><%= label_name %></span>
            <% end %>
          </div>
          <div style="margin-top: 4px;margin-left: 4px;">
            <%= task.name %>
          </div>
        </td>
        <td><%= I18n.l(task.created_at, format: :default) %></td>
        <td><%= I18n.l(task.due_at, format: :default) %></td>
        <td><%= task.progress_i18n%></td>
        <td><%= link_to '詳細', task %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'タスクの新規作成', new_task_path , class: "btn btn-info btn-lg btn-blok"%>
