<%= form_with url: tasks_path, model: @task do |form| %>
  <%= render partial: 'form', locals: { form: form } %>
  <p><%= form.submit '作成', class: 'btn btn-primary' %></p>
<% end %>
<p><%= link_to '一覧に戻る', tasks_path, class: 'btn btn-success mr-2' %></p>