<%= render 'layouts/error_messages', errors: errors %>
<table>
  <tr>
    <th><%= form.label :name, Task.human_attribute_name(:name) %></th>
    <td><%= form.text_field :name %></td>
  </tr>
  <tr>
    <th><%= Task.human_attribute_name(:status) %></th>
    <td><%=
      form.select :status,
      Task.status.options,
      include_blank: I18n.t('dictionary.words.select_indicate')
    %></td>
  </tr>
  <tr>
    <th><%= Task.human_attribute_name(:period) %></th>
    <td>
      <%= form.datetime_field :start_at, class: 'd-inline-block' %>
       〜 
      <%= form.datetime_field :due_date_at, class: 'd-inline-block' %>
    </td>
  </tr>
  <tr>
    <th><%= form.label :description, Task.human_attribute_name(:description) %></th>
    <td><%= form.text_area :description %></td>
  </tr>
  <tr>
    <th><%= form.label :labels, I18n.t('tasks.edit.label') %></th>
    <td>
      <%= form.text_area :labels, value: label_names, placeholder: I18n.t('tasks.edit.label_placeholder') %>
    </td>
  </tr>
</table>
<p><%= form.submit @submit_label, class: 'btn btn-primary' %></p>
