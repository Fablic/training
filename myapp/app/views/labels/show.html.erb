<p id="notice"><%= notice %></p>
<h1><%=t('actions.show') + Label.model_name.human() %></h1>
<p>
  <strong><%= Task.human_attribute_name(:name) %> </strong>
  <%= @label.name %>
</p>

<p>
  <strong><%= Task.human_attribute_name(:color) %></strong>
  <%= @label.color %>
</p>


<%= link_to t('actions.edit'), edit_label_path(@label) %> |
<%= link_to t('actions.back'), labels_path %>
