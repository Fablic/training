<%= render 'header' %>
<p style="color: green"><%= notice %></p>
<div class="container-sm rounded-2" style="padding:10px;">
  <h1><%= I18n.t('labels.index.title') %></h1>
  <hr>
</div>

<%= render 'labels_list', labels: @labels %>
