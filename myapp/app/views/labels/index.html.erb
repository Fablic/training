<div class="section">

<h1 class="title has-text-primary">
  <%= Label.model_name.human + t('actions.list')%>
  <%= link_to "#{t('actions.new')} #{Label.model_name.human}", new_label_path, class:"button is-link"  %>
  
</h1>
<div class='box'>



<div class="container"><%= paginate @labels %></div>
<%= page_entries_info @labels %>
<hr/>
<table class="type06">
  <thead>
    <tr>
      <th><%= sort_link(@q, :id)%></th>
      <th><%= sort_link(@q, :name)%></th>
      <th><%= sort_link(@q, :color)%></th>

      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @labels.each do |label| %>
      <tr>
        <td><%= label.id %></td>
        <td><%= label.name %></td>
        <td><%= label.color %></td>
        <td><%= link_to t('actions.show'), label , class: "button is-link" %></td>
        <td><%= link_to t('actions.edit'), edit_label_path(label) , class: "button is-link" %></td>
        <td><%= link_to t('actions.destroy'), label, method: :delete, class: "button is-danger", data: { confirm: t('messages.delete_confirm') } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>



</div>
</div>