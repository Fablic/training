<h1 class='text-center'><%= t('.title') %></h1>
<%= render 'flash' %>

<%= render 'form', task: @task, submit_button: t('tasks.new.create_button') %>

<%= link_to t('attributes.back'), tasks_path %>

<%= link_to 'back', tasks_path, class: "btn btn-secondary margin-left" %>

<style>
.margin-left {
  margin-left: 20px;
}
</style>