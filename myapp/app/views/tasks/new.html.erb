<h1><%= t ".title" %></h1>

<%= form_with(model: @task, local: true) do |t| %>
   <%= render "form", t: t %>
   <%= t.submit %> 
<% end %>

<%= link_to t('tasks.common.move_task_list'), root_path %>

