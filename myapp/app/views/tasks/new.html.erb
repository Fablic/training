<main class="container">
  <%= link_to sanitize("&larr; #{t('task_back')}"), tasks_path %>

  <div class="header">
    <h1>
      <%= t('pages.create_tasks_page.title') %>
    </h1>
  </div>

  <%= render "form", task: @task %>
</main>
