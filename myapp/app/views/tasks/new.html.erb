<h1><%= t ".title" %></h1>
<%= render "error_form", task: @task %>

<%= form_for @task do |t| %>
   <%= render "form", t: t %>
   <%= t.submit %> 
<% end %>

<%= link_to t('tasks.common.move_task_list'), root_path %>
