<h1><%= t('.title') %></h1>
<%= render 'flash' %>

<h3><%= @task.title %></h3>
<p><%= @task.description %></p>
<p><%= t("activerecord.attributes.task.statuses.#{@task.status}") %></p>

<p><%= link_to t('tasks.index.title'), tasks_path %></p>
