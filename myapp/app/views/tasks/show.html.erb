<h1><%= t '.title' %></h1>
<%= h(flash[:success]) %>

<p><%= t 'tasks.label.name' %></p>
<%= @task.name %>
<p><%= t 'tasks.label.description' %></p>
<%= @task.description %>
<p><%= t 'tasks.label.deadline_at' %></p>
<%= @task.deadline_at %>
<!--Bootstrapでデザインを入れる時にヘッダーで共通化予定-->
<p><%= link_to '一覧に戻る', root_path, id: 'back_root_link' %></p>
