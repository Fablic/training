<h1><%= t('tasks.edit.page_title') %></h1>

<%= render 'form', task: @task %>
<br>

<%= link_to t('common.link.tasks.back_to_show'), @task %>
<br>

<%= link_to t('common.link.tasks.back_to_index'), tasks_path %>
