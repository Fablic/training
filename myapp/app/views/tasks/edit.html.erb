<div class="container">
  <h1 class="text-center"><%= t('page_titles.tasks_update') %></h1>
  <%= render 'form', task: @task, labels: @labels, form_url: task_path(@task), form_method: :patch, submit_label: t('task.submit_labels.update_task') %>
</div>
