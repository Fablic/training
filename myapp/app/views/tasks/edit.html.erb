<main class="container">
  <%= link_to sanitize("&larr; #{t('back')}"), tasks_path %>

  <div class="header">
    <h1>
      <%= t('pages.edit_tasks_page.title') %>
    </h1>
  </div>

  <%= render "form", task: @task %>
</main>
