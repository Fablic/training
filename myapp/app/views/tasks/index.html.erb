<div class="alert alert-success" style="color: green"><%= notice %></div>

<h1><%= I18n.t('page_titles.tasks.index') %></h1>
<table>
  <thead>
    <tr>
      <th><%= Task.human_attribute_name(:name) %></th>
      <th><%= Task.human_attribute_name(:status) %></th>
    </tr>
  </thead>

  <tbody>
    <% @tasks.each do |task| %>
      <tr>
        <td><%= task.name %></td>
        <td><%= I18n.t("enums.task.status.#{task.status}") %></td>
        <td><%= link_to I18n.t('buttons.tasks.show'), task_path(task.id) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>
<%= paginate @tasks %>

<%= link_to I18n.t('buttons.tasks.new'), new_task_path %>

<div>
<%= form_with url: search_path, method: :get, local: true do |f| %>
  <p>検索フォーム</p>
    <p style="display: inline">検索ワード</p>
    <%= f.text_field :name %>
    <br>
    <p style="display: inline">ステータス</p>
    <%= f.select :status, Task.statuses.keys.map { |k| [I18n.t("enums.task.status.#{k}"), k] } , include_blank: true %>
  <br>
  <%= f.submit '検索' %>
<% end %>
</div>
