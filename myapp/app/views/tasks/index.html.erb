

<h2>Search</h2>
<%= form_with(url: tasks_path, method: :get, local: true) do %>
  <div class="row mb-3">
    <%= label_tag :title, "Title", class:'col-sm-2 col-form-label' %>
    <div class="col-sm-10"><%= text_field_tag :title, params[:title], class:'form-control' %></div>
  </div>
  <div class="row mb-3">
    <%= label_tag :status, "Status", class:'col-sm-2 col-form-label' %>
    <div class="col"><%= select_tag :status, options_for_select([['All', '']] + Task.statuses.keys.map { |k| [k.humanize, k] }, params[:status]), class:'form-control' %></div>
  </div>
  <%= submit_tag "Search", class:'btn btn-sm btn-success' %>
<% end %>


<h2>Tasks</h2>
<% flash.each do |key, message| %>
  <div class="flash <%= key %>"><%= message %></div>
<% end %>

<% if @tasks.empty? %>
  <p><%= t('tasks.no_tasks') %></p>
<% end %>

<table class="table table-striped table-bordered border-dark" id="result">
  <thead>
  <tr>
    <th>Title</th>
    <th>Description</th>
    <th>Priority</th>
    <th>Labels</th>
    <th><%= sortable "due_date", "DueDate" %></th></th>
    <th>Status</th>
    <th><%= sortable "created_at", "Created" %></th>
    <th><%= sortable "updated_at", "Updated" %></th>
    <th></th>
  </tr>
  </thead>
  <tbody>
  <% @tasks.each do |task| %>
    <tr>
      <td><%= link_to task.title, task %></td>
      <td><%= task.description %></td>
      <td><%= task.priority %></td>
      <td>N/A</td>
      <td><%= task.due_date %></td>
      <td><%= task.status %></td>
      <td><%= task.created_at %></td>
      <td><%= task.updated_at %></td>
      <td>
        <div class="d-grid gap-2 d-md-flex">
        <%= button_to "Update", edit_task_path(task), method: :get, class:'btn btn-sm btn-primary'%>
        <%= button_to "Delete", task_path(task), method: :delete, form: { data: { turbo_confirm: "Really delete?" }}, class:'btn btn-sm btn-danger'%>
        </div>
      </td>
    </tr>
  <% end %>
  </tbody>
</table>

<%= paginate @tasks, params: { search:params[:search], status: params[:status] }, theme: 'bootstrap-5' %>
<%= button_to "Create", "/tasks/new" , method: :get, class:'btn btn-sm btn-primary'%>
