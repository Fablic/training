<%= form_for(@task) do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name, placeholder: "タスク名" %>
    <%= f.label :description %>
    <%= f.text_area :description, placeholder: "タスク内容" %>
    <%= f.submit "Post", class: "btn btn-primary", id: "post_task_button" %>
<% end %>
