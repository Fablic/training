<div class="search_form">
  <%= form_with url: tasks_path, local: true, method: :get do |form| %>
    <%= form.text_field :title, id: 'search_word' %>
    <%= form.select :status, Task.statuses.keys.map { |k| [t("activerecord.enums.task.status.#{k}"), k] }, { default: :not_started } %>
    <%= form.submit '検索', id: 'search_submit' %>
  <% end %>
  </div>
</div>
