<%= form_with(url: tasks_path, method: :get, local: true) do |form| %>
  <div>
    <%= form.label :sort, 'ソート', style: "display: block" %>
    <%= form.select :sort, [
			['ID昇順(デフォルト)', 'id_asc'],
			['ID降順', 'id_desc']
		] %>
  </div>

  <div>
    <%= form.submit '送信' %>
  </div>
<% end %>
