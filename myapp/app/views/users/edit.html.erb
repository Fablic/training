<%= render 'header' %>
<div class="container-sm rounded-2" style="padding:10px;">
  <h1><%= I18n.t('users.edit.title', user_name: @user.name) =%></h1>
  <hr>
</div>

<%= render "form", user: @user %>

<br>

<%= render 'footer', user: @user %>
