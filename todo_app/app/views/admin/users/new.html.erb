<%= render 'layouts/error', model: @user %>
<%= form_with(model: @user, url: admin_users_path, method: :post, class: 'card w-50 m-2 p-2 gap-3') do |f| %>
  <%= render 'admin/users/form', f: f %>
  <div class="mx-auto">
    <%= f.submit I18n.t(:'button.create'), class: 'btn btn-primary' %>
  </div>
<% end %>
